# Apply once per namespace (copy to overlay or use SealedSecrets / ExternalSecrets).
apiVersion: v1
kind: Secret
metadata:
  name: platform-hook-secrets
type: Opaque
stringData:
  slack-webhook-url: https://hooks.slack.com/services/REPLACE/REPLACE/REPLACE
