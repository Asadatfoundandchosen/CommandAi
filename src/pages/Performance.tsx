import { PerformanceDashboard } from '@/components/PerformanceDashboard';

/** Web Vitals / performance budget dashboard (lazy route). */
export default function Performance() {
  return <PerformanceDashboard />;
}
