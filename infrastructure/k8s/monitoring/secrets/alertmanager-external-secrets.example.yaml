# Keys must match alertmanager-routing.example.yaml api_url_file / routing_key_file paths.
# Create in the same namespace as Alertmanager (e.g. monitoring).
apiVersion: v1
kind: Secret
metadata:
  name: alertmanager-external-secrets
  namespace: monitoring
type: Opaque
stringData:
  slack_api_url: "https://hooks.slack.com/services/REPLACE/REPLACE/REPLACE"
  pagerduty_routing_key: "REPLACE_PAGERDUTY_EVENTS_API_V2_ROUTING_KEY"
